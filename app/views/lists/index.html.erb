<% if signed_in? %>
  <h3><%= link_to "Add a list", new_list_path %></h3>
  <br />
<% end %>
<h2>Lists</h2>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %> -
      Party for: <%= list.serves %> people
    </li>
  <% end %>
</ul>
